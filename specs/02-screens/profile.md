# Профиль — `/profile`

**Тип:** Server Component. **Низ:** `<TabBar active="profile">`.

## Данные

`GET /api/profile`, `GET /api/subscription`

## Layout

Аватар (инициалы, золотая рамка) + имя + телефон. Карточка "Текущий тариф". Кнопки: "Продлить абонемент" → `<Link href="/plans">`; "История абонементов" → `<Link href="/history">`; "Настройки" → заглушка (без действия, mock-этап); "Выход" → заглушка (без действия, mock-этап, авторизации ещё нет).
